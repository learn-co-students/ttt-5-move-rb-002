#!/usr/bin/env ruby

require_relative '../lib/move.rb'

puts "Welcome to Tic Tac Toe!"

board = Array.new(9, " ")

puts "Where would you like to go?"

input = gets.strip # gets input form user

move(board,input)  #move function call

display_board(board) # display board after one round